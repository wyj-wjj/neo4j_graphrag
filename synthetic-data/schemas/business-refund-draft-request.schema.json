{
  "additionalProperties": false,
  "properties": {
    "amount": {
      "anyOf": [
        {
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        {
          "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d{0,2}0*$",
          "type": "string"
        }
      ],
      "title": "Amount"
    },
    "idempotency_key": {
      "maxLength": 200,
      "minLength": 8,
      "title": "Idempotency Key",
      "type": "string"
    },
    "order_id": {
      "title": "Order Id",
      "type": "string"
    }
  },
  "required": [
    "order_id",
    "amount",
    "idempotency_key"
  ],
  "title": "RefundDraftRequest",
  "type": "object"
}
