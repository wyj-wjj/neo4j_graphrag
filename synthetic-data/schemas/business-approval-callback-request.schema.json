{
  "additionalProperties": false,
  "properties": {
    "decision": {
      "enum": [
        "approved",
        "rejected"
      ],
      "title": "Decision",
      "type": "string"
    },
    "idempotency_key": {
      "maxLength": 200,
      "minLength": 8,
      "title": "Idempotency Key",
      "type": "string"
    }
  },
  "required": [
    "decision",
    "idempotency_key"
  ],
  "title": "ApprovalCallbackRequest",
  "type": "object"
}
